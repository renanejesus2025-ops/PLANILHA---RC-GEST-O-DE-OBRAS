[pytest]
; Permite `import src...` e `import config...` a partir da raiz do
; repositório sem precisar instalar o projeto como pacote (setup.py/
; pyproject.toml de empacotamento fica fora do escopo desta etapa).
pythonpath = .
testpaths = testes
